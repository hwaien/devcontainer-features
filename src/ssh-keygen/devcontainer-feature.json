{
  "name": "Generate SSH Key",
  "id": "ssh-keygen",
  "version": "0.0.3",
  "description": "A feature to generate an SSH key for the dev container.",
  "options": {
    "path": {
      "type": "string",
      "default": "",
      "description": "Enter a path for the SSH key."
    },
    "passphrase": {
      "type": "string",
      "default": "",
      "description": "Enter a passphrase for the SSH key."
    }
  }
}
